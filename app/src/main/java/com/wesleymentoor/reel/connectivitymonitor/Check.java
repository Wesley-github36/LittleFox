package com.wesleymentoor.reel.connectivitymonitor;

public class Check {

    void functions() {
        NetDetect netDetect = new NetDetect();

        netDetect.checkInternetConnection(isConnected -> {

        });
    }
}
